storage:
 value: default
 wizard:
   name: "Storage Configuration"
   kind: cluster_storage

dataset:
 value: mnist
 version: '1.0.0'
 workspace: 'kuberlab-demo'
 wizard:
   name: "Mnist dataset configuration"
   kind: dataset

spark_master: spark://spark-master.spark:7077

packages:
 - "git+https://github.com/kuberlab/python-mlboardclient.git"